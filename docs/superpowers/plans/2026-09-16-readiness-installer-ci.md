# Installer guidance and repository verification — bounded implementation

Approved by the owner's request to implement the product review. Existing data and authorization remain unchanged.

1. Add a **read-only guide** at `/setup/`, URL name `setup-guide`, view module `open_marketplace.web.setup_views`, template `setup.html`. This is guidance through the existing secure setup, NOT a first-visitor admin registration feature. No database writes, no env values or account inventory exposed, no mutation forms. Give clear ordered steps and real links to existing registration/login/security/seller/catalog/admin routes, explain synthetic data, local Mailpit, explicit participant admission, and the meaning of localhost.
2. Keep first security-admin bootstrap using the existing documented command and protected invitation/2FA flow. Do not bypass/bootstrap/grant real rights automatically. Copy only verified existing command syntax from README/runbooks/source. Explain that secrets are never placed in chat.
3. Add test-first regressions in `web/tests/test_setup.py`: GET succeeds, one main heading, no mutation form, correct real links, no misleading claims about live orders/payments or completed setup, POST not accepted. No need for DB access just to show the guide.
4. Add a GitHub Actions workflow for source pushes to main and pull requests. Minimal permissions (`contents: read`), pinned official action commit, credential persistence off, no pull_request_target, no repository secrets, no deployment, no write/comment/PR permissions, standard hosted Linux runner only. Run actual existing project checks using Docker test services with generated ephemeral .env, never publicize that file or values. Check migrations, module import boundaries, all Django tests including browser/semantic integration; use the project's restore proof if feasible and keep its safety boundary. Syntax and commands must be exercised locally where possible; do not claim a hosted CI run without an actual run URL.
5. Add/update RU/EN installation docs to point to this guide and accurately distinguish: public source download, local running demonstration, closed test catalog, real-commerce blockers. A missing copyright licence must be described as an unresolved grant of reuse rights, not silently fixed by adding MIT/Apache or fabricated copyright ownership. A concise repository code-use status notice with official GitHub documentation reference is allowed; do not grant any new rights or make legal/production-readiness guarantees.
6. Run focused guide and structural checks; retain evidence in `artifacts/readiness/installer-code/`. Main Hermes performs full integration, runtime tests and publication separately.

Ownership for this package: `open_marketplace/web/setup_views.py`, `open_marketplace/templates/setup.html`, `open_marketplace/web/tests/test_setup.py`, `open_marketplace/config/urls.py` (ONLY import and setup route), `.github/workflows/ci.yml`, `README.md`, `README.en.md`, RU/EN catalog quickstart runbooks, `docs/runbooks/initial-setup-ru.md`, `docs/runbooks/initial-setup-en.md`, `docs/code-use-status.md`. Do not edit application/domain modules, templates/base.html, templates/home.html, config/settings.py, Dockerfile/Compose/ops scripts, dependency files, or other tests.
